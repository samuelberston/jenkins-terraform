environment = "dev"  # or "prod" depending on your needs
aws_region  = "us-west-1"  # adjust if you want a different region

# Remove or comment out the key_name variable since it will be generated dynamically
# key_name    = "jenkins-key"

# Optional: Override other defaults if needed
# vpc_cidr = "10.0.0.0/16"
# availability_zones = ["us-west-1a", "us-west-1b"]
# private_subnets = ["10.0.1.0/24", "10.0.2.0/24"]
# public_subnets = ["10.0.101.0/24", "10.0.102.0/24"]
# admin_cidr_blocks = ["YOUR.IP.ADDRESS/32"]  # Replace with your IP for security 